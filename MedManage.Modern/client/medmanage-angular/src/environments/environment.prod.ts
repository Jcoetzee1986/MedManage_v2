export const environment = {
  production: true,
  apiUrl: '/api',
  identityServerUrl: '/identity',
  // Session timeout in minutes (warning will show 5 minutes before timeout)
  sessionTimeoutMinutes: 30
};
